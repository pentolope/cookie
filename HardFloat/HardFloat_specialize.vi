/*
Minimal HardFloat specialization settings for this project.
These defaults keep all round/exception behavior enabled and do not
enable NaN payload propagation extensions.
*/

`define HardFloat_signDefaultNaN 1'b0
`define HardFloat_fractDefaultNaN(sigWidth) {1'b1, {(sigWidth - 2){1'b0}}}
